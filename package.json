{
  "name": "cem-plugin-react-wrapper",
  "version": "0.1.0",
  "description": "`A custom elements manifest analyzer plugin to generate react wrappers for custom elements based on the custom elements manifest (CEM)`",
  "main": "index.js",
  "module": "index.js",
  "type": "module",
  "files": [
    "dist",
    "index.d.ts",
    "index.js"
  ],
  "scripts": {
    "start": "node --loader ts-node/esm --no-warnings dev.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.6.3",
    "@types/node": "^18.0.6",
    "@types/prettier": "^2.6.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
